@model IEnumerable<ListRoleViewModel>
@{
    ViewData["Title"] = "Get All Roles";
}

<h1>All Roles</h1>
@if (Model.Any())
{
    <form asp-controller="Person" asp-action="GetAllPersons" method="get">
        <table class="table table-striped table-light">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Role name</th>
                    <th scope="col">Options</th>
                </tr>
            </thead>
            <tbody>
                @foreach(var  role in Model)
                {
                    <tr>
                        <th scope="row">@role.Id</th>
                        <td>@role.Name </td>
                        <td>
                            <button class="btn-primary" asp-action="UpdateRole" asp-controller="Admin" asp-area="Admin" asp-route-id="@role.Id">Update</button>
                            <button class="btn-danger" asp-action="DeleteRole" asp-controller="Person" asp-area="Admin" asp-route-id="@role.Id">Delete</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </form>
}
else
{
    <div class="card">
        <div class="card-header">
            No roles created yet
        </div>
        <div class="card-body">
            <button class="btn-primary" style="width:auto">
                Create Role
            </button>
        </div>
    </div>

}

